import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    routingButtonWrapper: {
        marginTop: 10,
        borderRadius: 8,
        borderWidth: 0.5,
        borderColor: "#E9E9E9",
        backgroundColor: "#ffffff",
        justifyContent: "space-between",
        flexDirection: "column",
        alignItems: "stretch",
    },
});

export default styles;
